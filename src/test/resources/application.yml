spring:
  application:
    name: my-spring-boot-app
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB
  data:
    mongodb:
      host: localhost
      port: 27017
      database: ezer


server:
  port: 8080

jwt:
    secret: somesecret

aws:
  s3:
    bucketName: userprofilepicturebucket
  



logging:
  pattern:
    dateformat: "yyyy-MM-dd'T'HH:mm:ss.SSSZ, America/Chicago"
